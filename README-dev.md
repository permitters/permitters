### Setup

    gem install bundler
    bundle install

### Running tests

To ensure you are using the latest dependencies that we think should work with Permitters, use:

    rake

For faster tests:

    rake appraise